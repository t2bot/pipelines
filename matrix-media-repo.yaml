steps:
  - label: "🔨 Build :go: 1.14"
    commands: 
      - "./build.sh"
      - "chown -R buildkite-agent:buildkite-agent \"${BUILDS_PATH}/${AGENT_DIR}/${ORG_DIR}/${PIPELINE_DIR}\""
    plugins:
      - docker#v3.0.1:
          image: "golang:1.14"
          mount-buildkite-agent: false
  - label: "🤝 Complement"
    env:
      COMPLEMENT_BASE_IMAGE: complement-media-repo
      COMPLEMENT_VERSION_CHECK_ITERATIONS: 400
    commands: 
      - "docker build -t complement-media-repo -f Complement.Dockerfile ."
      - "chmod +x ci-complement.sh" # To fix the script
      - "./ci-complement.sh"
      - "chown -R buildkite-agent:buildkite-agent \"${BUILDS_PATH}/${AGENT_DIR}/${ORG_DIR}/${PIPELINE_DIR}\""
